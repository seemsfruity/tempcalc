import tkinter as tk
from tkinter import messagebox

def fahrenheit_to_celsius(f):
    return (f - 32) * 5 / 9

def celsius_to_fahrenheit(c):
    return (c * 9 / 5) + 32

def calculate_temperature():
    temp_input = entry_temp.get()
    try:
        temp_value = float(temp_input)
    except ValueError:
        messagebox.showerror("Invalid input", "Please enter a valid number.")
        return
    
    if conversion_var.get() == "F to C":
        result = fahrenheit_to_celsius(temp_value)
        messagebox.showinfo("Result", f"{temp_value} °F is {result:.2f} °C")
    else:
        result = celsius_to_fahrenheit(temp_value)
        messagebox.showinfo("Result", f"{temp_value} °C is {result:.2f} °F")

root = tk.Tk()
root.title("Temperature Converter")

label = tk.Label(root, text="Enter temperature:")
label.pack(pady=5)

entry_temp = tk.Entry(root)
entry_temp.pack(pady=5)

conversion_var = tk.StringVar(value="F to C")
frame = tk.Frame(root)
frame.pack(pady=5)

radio_f_to_c = tk.Radiobutton(frame, text="Fahrenheit to Celsius", variable=conversion_var, value="F to C")
radio_c_to_f = tk.Radiobutton(frame, text="Celsius to Fahrenheit", variable=conversion_var, value="C to F")
radio_f_to_c.pack(side="left", padx=10)
radio_c_to_f.pack(side="left", padx=10)

submit_btn = tk.Button(root, text="Submit", command=calculate_temperature)
submit_btn.pack(pady=10)

root.mainloop()
